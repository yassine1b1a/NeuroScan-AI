# NeuroScan AI — Brain Tumor MRI Classifier

Deep learning web app using EfficientNetB0 to classify brain MRI scans.

## Setup

```bash
pip install -r requirements.txt
# Download model.h5 from [link] and place in project root
python server.py